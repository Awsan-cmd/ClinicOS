# Fork Strategy
Current status: standalone public repository `ClinicOS`; no upstream relationship established.
If an upstream is discovered: verify it from Git metadata/GitHub, document it, isolate local changes, review upstream changes before applying, and never overwrite custom behavior blindly.
